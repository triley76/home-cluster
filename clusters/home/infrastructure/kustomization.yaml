apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# MetalLB Phase 1 installs the controller and speakers only.
# Address-pool configuration is enabled after the installation is validated.
resources:
  - metallb.yaml
